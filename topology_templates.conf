#@description A sample list of setups
#@key_label   "name"
#@meta_label  "type"

[name="chi2_fit_template" type="mygsl::histogram_1d"]

#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "\Chi^{2}_{fit}"

#@description The Y axis label
display.yaxis.label : string = "Events"

#@description The number of bins
number_of_bins : integer = 200

# #@description The unit of the bins' bounds (a standard unit, typically SI or CLHEP)
# unit : string = "ns"

# #@description The lower bound of the histogram
# min : real as time = 0.0 ns

# #@description The upper bound of the histogram
# max : real as time = 10.0 ns

#@description The lower bound of the histogram
min : real = 0.0

#@description The upper bound of the histogram
max : real = 200.0

[name="track_length_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "Track length [mm]"

#@description The Y axis label
display.yaxis.label : string = "Events"

#@description The number of bins
number_of_bins : integer = 100

# #@description The unit of the bins' bounds (a standard unit, typically SI or CLHEP)
# unit : string = "mm"

# #@description The lower bound of the histogram
# min : real as length = 0.0 mm

# #@description The upper bound of the histogram
# max : real as length = 10.0 mm

#@description The lower bound of the histogram
min : real = 0.0

#@description The upper bound of the histogram
max : real = 2000.0

[name="tof_log_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "log(\Chi^{2}_{int})"

#@description The Y axis label
display.yaxis.label : string = "Events [A.U.]"

#@description The number of bins
number_of_bins : integer = 100

#@description The lower bound of the histogram
min : real = -50.0

#@description The upper bound of the histogram
max : real = 0.0

[name="tof_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "\Delta t [ns]"

#@description The Y axis label
display.yaxis.label : string = "dN/d\Delta t [A.U.]"

#@description The number of bins
number_of_bins : integer = 100

#@description The unit of the bins' bounds (a standard unit, typically SI or CLHEP)
unit : string = "ns"

#@description The lower bound of the histogram
min : real as time = 0.0 ns

#@description The upper bound of the histogram
max : real as time = 10.0 ns

[name="tof_probability_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "TOF probability"

#@description The Y axis label
display.yaxis.label : string = "Events [A.U.]"

#@description The number of bins
number_of_bins : integer = 200

#@description The lower bound of the histogram
min : real = 0.0

#@description The upper bound of the histogram
max : real = 1.0

[name="delta_vertices_Y_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "#Delta_{y}"

#@description The Y axis label
display.yaxis.label : string = "Events [A.U.]"

#@description The number of bins
number_of_bins : integer = 100

#@description The lower bound of the histogram
min : real = -500

#@description The upper bound of the histogram
max : real = 500

[name="delta_vertices_Z_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "#Delta_{z}"

#@description The Y axis label
display.yaxis.label : string = "Events [A.U.]"

#@description The number of bins
number_of_bins : integer = 100

#@description The lower bound of the histogram
min : real = -500

#@description The upper bound of the histogram
max : real = 500

[name="cos_angle_template" type="mygsl::histogram_1d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
linear : boolean = 1

#@description The X axis label
display.xaxis.label : string = "Cos(#theta)"

#@description The Y axis label
display.yaxis.label : string = "Events [A.U.]"

#@description The number of bins
number_of_bins : integer = 100

#@description The lower bound of the histogram
min : real = -1

#@description The upper bound of the histogram
max : real = 1

[name="chi2_probability_template" type="mygsl::histogram_2d"]
#@config The histogram template of the time of flight distribution

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
x.linear : boolean = 1

#@description The X bin unit
x.unit : string = "ns"

#@description The lower bound of the histogram
x.min : real = 0

#@description The upper bound of the histogram
x.max : real = +1.0

#@description The upper bound of the histogram
x.number_of_bins : integer = 1000

#@description The linear mode (default)
y.linear : boolean = 1

#@description The X bin unit
y.unit : string = "ns"

#@description The lower bound of the histogram
y.min : real = 0.0

#@description The upper bound of the histogram
y.max : real = +10.0

#@description The upper bound of the histogram
y.number_of_bins : integer = 1000

#@description The X axis label
display.xaxis.label : string = "\Chi^{2}_{int}"

#@description The Y axis label
display.yaxis.label : string = "\Chi^{2}_{ext}"

[name="vertex_distribution_template" type="mygsl::histogram_2d"]
#@description The title of the histogram (optional)
title : string = ""

#@description The group of the histogram (optional)
group : string = "__template"

#@description The build mode (default : "regular", "table", "mimic");
mode : string = "regular"

#@description The linear mode (default)
x.linear : boolean = 1

#@description The X bin unit
x.unit : string = "mm"

#@description The lower bound of the histogram
x.min : real = -2500.0

#@description The upper bound of the histogram
x.max : real = +2500.0

#@description The upper bound of the histogram
x.number_of_bins : integer = 100

#@description The linear mode (default)
y.linear : boolean = 1

#@description The X bin unit
y.unit : string = "mm"

#@description The lower bound of the histogram
y.min : real = -1500.0

#@description The upper bound of the histogram
y.max : real = +1500.0

#@description The upper bound of the histogram
y.number_of_bins : integer = 100

#@description The X axis label
display.xaxis.label : string = "y"

#@description The X axis unit for display (a standard unit, typically SI or CLHEP)
display.xaxis.unit : string = "mm"

#@description The Y axis label
display.yaxis.label : string = "z"

#@description The Y axis unit for display (a standard unit, typically SI or CLHEP)
# display.yaxis.unit : string = "mm"
